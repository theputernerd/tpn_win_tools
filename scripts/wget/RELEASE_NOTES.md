v0.3.8
- Adaptive multi-threading with --auto-threads and tuning flags
- Status mode to report progress from .par.parts files
- Configurable segment sizes with K/M/G parsing and larger default (5MB)
- Version flag support (wget --version)
