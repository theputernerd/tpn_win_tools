# Design Logs

Design exploration, architecture notes, tradeoffs, and decisions that persist
across sessions. Required before coding on **high-risk** tasks, and whenever the
direction is not already settled.

Create one: `instructions/helpers/create-design-log.sh <slug>`
