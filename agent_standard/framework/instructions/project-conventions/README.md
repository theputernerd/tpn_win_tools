# Project Conventions

Repo-specific conventions that are too specific for `global-conventions.md` but
apply across many tasks. Put project customisations here so `AGENTS.md` remains
a clear general contract. The initializer never upgrades an installed project;
future framework changes must be reviewed and applied manually.

Create one: `instructions/helpers/create-project-convention.sh <slug>`
